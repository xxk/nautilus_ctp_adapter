# <变更名称> 设计

> 默认不复制到新 change，需手工创建。
>
> 仅在满足以下任一门槛时才建议创建：
>
> 1. 存在两个及以上可行方案且需要明确取舍
> 2. 正式入口或主要实现落点容易改错
> 3. 涉及发布、部署、迁移或回滚策略
> 4. 需要提前冻结长期接口、状态文件、目录归属或返回码口径

**状态**：待评审
**日期**：YYYY-MM-DD
**范围**：<影响目录/模块>
**关联 plan**：./plan.md

## 一、现状

说明当前系统如何工作，以及为什么还不能满足目标。

## 二、正式入口与实现落点

1. 这次应该改哪些正式入口或正式实现文件
2. 哪些文件只是诊断、兼容壳或临时脚本，不应作为主落点
3. 新能力应落在哪一层目录

## 三、设计方案

说明推荐方案、关键模块改动、数据或状态产物、异常处理口径以及与现有实现的衔接方式。

## 四、接口与输入输出

若涉及脚本、CLI、配置、JSON、状态文件或返回码，应明确输入输出约束。

## 五、AI 实现约束

1. 不得引入哪些兼容、兜底或静默失败逻辑
2. 是否允许保留旧入口
3. 哪些行为必须 Fail Fast

## 六、备选方案

至少写出一个备选方案，并说明为什么不选。

## 七、风险与影响面

从功能、数据、测试、远程环境和兼容性角度说明风险。

## 八、发布回滚与退出策略

这里只讨论发布、部署、迁移层面的退出策略，不等于运行时 fallback。

## 九、需要沉淀为长期规则的内容

提前列出完成后应回写到长期文档的设计口径。
